export type IDate = string;

export interface IAddress {
    // country: string;
    zipCode: string;
    city: string;
}

export interface IUser {
    firstName: string;
    lastName: string;
    email: string;
}